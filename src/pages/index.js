// here we are maintaing the one-main file of all the components or pages:
// so that we can have idea or track on every component or page of our application:
import HomePage from "./HomePage";
import ProductsPage from './ProductsPage';
import ProductDetailPage from "./ProductDetailPage";

// here we are exporting all the components or pages:
// which we have created for our application:
// In the form of one export object:
export{HomePage,ProductDetailPage,ProductsPage};